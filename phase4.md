## 📄 phase4.md: Strategy UX & Detail Experience

현재 구현 상태는 `IMPLEMENTATION_STATUS.md`에서 관리합니다.

### 1. 목표 및 설계 원칙

- **Goal:** 전략 탐색, 상세 분석, 거래 이력 확인 경험을 실제 데이터 기반 UI로 완성한다.
- **Data Visualization:** 단위가 다른 지표를 왜곡 없이 비교할 수 있어야 한다.
- **Correction:** 종목 상세는 Mock 전용 샘플 화면이 아니라 실제 선택 종목 + 실제 거래 이력 화면이어야 한다.

### 2. 세부 수행 작업

1. 전략/대시보드/상세 화면을 provider 기반 실제 데이터와 연결한다.
2. 상세 페이지 레이아웃을 구현한다.
   - 상단 Glass 카드
   - 중앙 Radar 차트
   - 하단 거래 이력 타임라인
3. 정규화 차트 로직을 구현한다.
   - `PER`: 반전 정규화
   - `ROE`, `DividendYield`: 일반 정규화
4. 스마트 태그 시스템을 구현한다.
   - `PER < 10` -> `#저평가`
   - `ROE > 15` -> `#우량주`
   - `DividendYield > 4` -> `#고배당`
5. SQLite 거래 이력을 `ticker` 기준으로 필터링해 상세 화면 타임라인에 연결한다.
6. 차트와 스크롤 성능을 점검하고 개선한다.

### 3. 검증 및 디버깅

- PER과 ROE 단위 차이 때문에 차트가 찌그러지지 않아야 한다.
- 여러 태그가 중복 없이 보기 좋게 나열되어야 한다.
- 상세 화면에서 실제 선택 종목 데이터와 해당 종목의 거래 이력이 보여야 한다.
- 거래 내역이 많아져도 스크롤이 과도하게 끊기지 않아야 한다.
