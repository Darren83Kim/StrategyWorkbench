## 📄 phase5.md: Monetization & Final Fail-safe

현재 구현 상태는 `IMPLEMENTATION_STATUS.md`에서 관리합니다.

### 1. 개요 및 설계 원칙

- **Goal:** 광고를 핵심 사용자 흐름을 깨지 않는 방식으로 연동하고 릴리즈 준비를 마무리한다.
- **Monetization:** 하단 배너 + 전략 저장 시 전면 광고를 적용한다.
- **Fail-safe:** 광고 로드 실패나 네트워크 문제로 사용자가 멈춰 있지 않도록 한다.

### 2. 세부 수행 작업

1. `google_mobile_ads`를 추가하고 플랫폼별 App ID를 등록한다.
2. 하단 고정 배너 광고 위젯을 실제 레이아웃에 배치한다.
   - 앱 첫 프레임을 막지 않도록 배너 로드는 첫 화면 렌더링 후 지연 실행한다.
3. 전면 광고 시스템을 구현한다.
   - 전략 저장 버튼과 연결
   - 개발 빌드 테스트 ID
   - 릴리즈 빌드 실제 ID
4. Fail-safe를 구현한다.
   - 500ms 타임아웃
   - `onAdFailedToLoad` 또는 동등한 실패 처리
   - 실패 시 즉시 저장 완료 흐름 진행
5. 빈도 제한을 구현한다.
   - 최근 1시간 내 재노출 금지
6. 앱 아이콘, 스플래시, 릴리즈 설정을 정리한다.

### 3. 검증 및 디버깅

- 비행기 모드에서도 전략 저장 흐름이 0.5초 안에 계속 진행되어야 한다.
- 개발 빌드에서 구글 테스트 광고 ID가 사용되어야 한다.
- 앱 실행 -> 데이터 로드 -> 전략 생성 -> 전략 저장(광고) -> 알림 수신까지 기본 시나리오가 막힘 없이 이어져야 한다.

### 4. 구현 전 준비물

#### 바로 개발 가능한 최소 조건

- 개발 단계에서는 Google 테스트 App ID / 테스트 ad unit ID로 먼저 구현 가능하다.
- 이 경우 실제 수익화는 되지 않지만 UI/흐름/Fail-safe는 검증할 수 있다.

#### 실제 AdMob 연동에 필요한 사용자 준비물

1. AdMob 계정
2. Android 앱용 AdMob App ID
3. iOS 앱용 AdMob App ID
4. Android 배너 광고 ad unit ID
5. iOS 배너 광고 ad unit ID
6. Android 전면 광고 ad unit ID
7. iOS 전면 광고 ad unit ID
8. 앱의 최종 패키지명 / 번들 ID 확정
9. 배포용으로 사용할 앱 스토어 등록 계획
10. 가능하면 앱에 연결할 developer website

#### 운영 전 참고

- 2025년부터 새 앱은 `app-ads.txt` 기반 앱 검증이 요구될 수 있으므로, 배포 전 developer website와 `app-ads.txt` 준비를 같이 고려한다.
- 개발 중에는 반드시 테스트 광고를 사용하고, 릴리즈 직전에만 실제 ad unit ID로 교체한다.

### 5. 현재 구현 상태 메모

- 2026-04-20 기준 테스트 광고를 사용하는 Android/iOS 구조가 코드에 반영되었다.
- `BannerAdWidget`, `AdService`, `FilterCreationScreen` 저장 흐름, `RootLayout` 하단 레이아웃 연결까지 완료되었다.
- 2026-04-21 기준 광고 SDK는 앱 시작 시 강제 초기화하지 않고, 배너/전면 광고 필요 시점에 초기화하도록 조정되었다.
- 2026-04-21 기준 디버그 빌드에서는 배너 광고 로드를 지연시켜 시작 구간 jank를 줄인다.
- 2026-04-21 기준 debug 런타임에서도 기본적으로 테스트 광고가 표시되며, 성능 진단이 필요할 때는 `--dart-define=DISABLE_DEBUG_ADS=true`로 광고 초기화/로드를 끌 수 있다.
- 실제 운영 수익화 단계로 가려면 사용자 제공 AdMob 값과 배포 준비물이 필요하다.
