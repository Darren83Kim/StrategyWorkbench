## 📄 phase3.md: Strategy Engine & Fail-safe Notification

현재 구현 상태는 `IMPLEMENTATION_STATUS.md`에서 관리합니다.

### 1. 목표 및 설계 원칙

- **Goal:** 전략 점수 계산, 순위 비교, 이탈 감지, 로컬 알림까지 하나의 전략 파이프라인으로 완성한다.
- **Scoring Engine:** 사용자 가중치 기반 순위를 계산하되, 비정상 데이터에서도 앱이 멈추지 않아야 한다.
- **Correction:** 민감도는 단순 고정 rank 숫자가 아니라 전략 풀 대비 비율과 활성 전략 상태에 연결되어야 한다.

### 2. 세부 수행 작업

1. 스코어링 로직을 구현한다.
   - Min-Max 정규화
   - 가중치 합산
   - 정렬 및 순위 계산
2. 데이터 예외 처리를 구현한다.
   - `PER <= 0`, `ROE == null`, `dividendYield == null` 또는 비정상 값 대응
   - 제외 대신 `0점` 또는 `N/A` 성격으로 처리한다.
3. 전략 스냅샷과 순위 변동 비교 로직을 구현한다.
4. 민감도 시스템을 구현한다.
   - `High`: 상위 10%
   - `Medium`: 상위 20%
   - `Low`: 상위 30%
5. 로컬 알림 서비스를 구현한다.
6. WorkManager 기반 백그라운드 체크를 구현한다.
   - 실제 활성 전략
   - 실제 관심 종목/포트폴리오
   - 실제 캐시 데이터

### 3. 검증 및 디버깅

- PER 0, ROE null 같은 비정상 데이터에서도 스코어링이 중단되지 않아야 한다.
- 동일한 하락 상황에서 민감도에 따라 알림 발생 여부가 달라져야 한다.
- 백그라운드 태스크 강제 실행 시 실제 알림 파이프라인이 동작해야 한다.
